# Frontend environment configuration
# Copy to `.env` (or `.env.local`) and adjust for your setup.
# Used by Vite at build/dev time. Variables MUST be prefixed with VITE_ to be exposed.

# Backend base URL — used for both REST and WebSocket (ws:// is derived automatically).
VITE_API_URL=http://localhost:8000
