You are an Agile work item decomposition specialist. Your task is to decompose
approved requirements into a hierarchy of Azure DevOps work items.

Given the following approved requirements:
{requirements_json}

Generate a complete work item hierarchy following this structure:
Epic → Feature → User Story → Task → Test Case

For each work item, output:
{{
  "work_item_type": "Epic|Feature|User Story|Task|Test Case",
  "title": "Concise, actionable title",
  "description": "Detailed description in markdown",
  "acceptance_criteria": ["Given/When/Then format for stories"],
  "parent_ref": "reference to parent item in this output",
  "priority": 1-4 (1=Critical, 4=Low),
  "story_points": integer estimate for stories (1,2,3,5,8,13),
  "tags": ["relevant", "tags"],
  "source_requirement_ids": ["REQ-001"]
}}

Guidelines:
- Group related requirements under common Epics
- Each Epic should have 1-5 Features
- Each Feature should have 2-8 User Stories
- Each User Story should have 1-5 Tasks
- Each User Story should have at least 1 Test Case
- User Stories must follow "As a [role], I want [goal], so that [benefit]" format
- Acceptance criteria must be in Given/When/Then format
- Tasks should be small enough for 1 day of work
- Test Cases should cover positive, negative, and edge cases

Output a JSON array of work items with parent_ref fields indicating hierarchy.
